export const metadata = { title: 'Home — Palatable' };

export default function HomePage() {
  return (
    <div className="px-10 py-10 max-w-6xl">
      <h1 className="font-serif text-5xl mb-3">Home</h1>
      <p className="font-serif italic text-sm text-muted">
        Chef-shell scaffold landed. Full Looking Ahead + KPI + supplier
        context strip from chef-home-mockup-v5 ports in the next commit.
      </p>
    </div>
  );
}
